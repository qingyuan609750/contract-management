version: '3.8'

services:
  app:
    build: .
    ports:
      - "3001:10000"
    environment:
      - NODE_ENV=production
      - PORT=10000
    volumes:
      - ./data:/data
    restart: unless-stopped
